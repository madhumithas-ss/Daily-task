#include <stdio.h>

int main() {
    int num1, num2;

    printf("Enter two numbers: ");
    scanf("%d %d", &num1, &num2);

    if (num1 == num2)
        printf("True\n");   // Numbers are equal
    else
        printf("False\n");  // Numbers are not equal

    return 0;
}
#include <stdio.h>

int main() {
    int num, temp, digit;
    int count = 0, sum = 0, product = 1;

    // Input number
    printf("Enter a number: ");
    scanf("%d", &num);

    temp = num;  
    if (num < 0) {
        num = -num;
    }
    while (num > 0) {
        digit = num % 10;  